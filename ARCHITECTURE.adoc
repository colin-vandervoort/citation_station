ifdef::env-github[]
:MERMAID: source, mermaid
endif::[]
ifndef::env-github[]
:MERMAID: mermaid
endif::[]

= Citation Station Architecture
:toc:
:toclevels: 2
:sectnums:
:source-highlighter: highlight.js

== Requirements

* Expose a core crate with:
** structs for citation styles
** serde logic for each format
* Expose a CLI crate that consumes the core crate
* (eventually) build a web-based version of the tool

== Data modeling

[{MERMAID}]
----
classDiagram
    class Citation {
        &lt;&lt;Enumeration&gt;&gt;
        Book
        ConferencePaper
        ConferenceProceedings
        OnlineManual
        OnlineStandard
        OnlineThesis
        OnlineVideo
        PrintManual
    }

    class Bibliography {
        +Array#lt;Citation#gt; citations
    }

    Bibliography *-- Citation
----

Most data models are defined in the `citation_station/src/api` directory. See the link:./citation_station/src/api/media/book.rs[Book model] for an example.